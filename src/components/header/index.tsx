import React from 'react';

import './header.css';

const Header = () => {
    return (
        <header className="header basic-padding bg-color">
            <p>Eventify</p>
        </header>
    );
};

export default Header;